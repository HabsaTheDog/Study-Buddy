{
  "name": "uni-agent-study-buddy",
  "version": "0.1.0",
  "private": true,
  "description": "Local provider-neutral workspace for a FH Technikum Wien Moodle assistant.",
  "scripts": {
    "browser:install": "agent-browser install",
    "browser:install:linux-deps": "agent-browser install --with-deps",
    "moodle:login": "scripts/moodle_login.sh",
    "moodle:snapshot": "scripts/moodle_snapshot.sh",
    "moodle:courses": "scripts/moodle_course_index.sh",
    "moodle:sync": "scripts/moodle_sync.sh",
    "providers": "scripts/agent_provider_probe.sh",
    "quiz:assist": "scripts/quiz_assist.sh",
    "test": "python3 -m compileall src scripts"
  },
  "dependencies": {
    "agent-browser": "^0.26.0"
  }
}
